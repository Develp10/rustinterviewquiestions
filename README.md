# Rust Interview Deep Dive

Репозиторий с разбором задач и теоретических вопросов с собеседований на позиции Rust-разработчика уровня middle, senior и staff. Каждая задача оформлена как полноценный разбор от практикующего разработчика: условие, наивная попытка, объяснение почему компилятор недоволен или почему рантайм проседает, корректное решение с тестами и, где уместно, бенчмарк или `cargo asm`.

Это не сборник упражнений в духе «угадай вывод программы». Здесь сценарии, которые реально встречаются в продакшене: lock-free структуры, self-referential типы в async, FFI с тензорными библиотеками, корректный `Send` на гардах через await, memory ordering под `loom`, soundness кастомных коллекций.

## Структура репозитория

```
rust-interview-deepdive/
├── README.md
├── CONTRIBUTING.md
├── LICENSE
├── Cargo.toml                  # workspace
├── .github/workflows/ci.yml    # cargo test + clippy + fmt + miri
├── tasks/
│   ├── 01-ownership-move/
│   │   ├── README.md           # условие, разбор, подводные камни
│   │   ├── src/lib.rs          # реализация
│   │   ├── tests/it.rs         # интеграционные тесты
│   │   └── benches/bench.rs    # где уместно
│   └── ... до 40/
└── theory/
    ├── memory-model.md
    ├── send-sync.md
    ├── pin-unpin.md
    ├── async-runtime.md
    └── unsafe-invariants.md
```

## Уровни сложности

**middle** — задачи 1–10, типовые вопросы про владение, заимствование и трейты.

**senior** — задачи 11–30, конкурентность, async, unsafe, производительность.

**staff** — задачи 31–40, дизайн API, тёмные углы компилятора, макросы, FFI, soundness.

## Как работать с репозиторием

Сначала читайте условие задачи и пробуйте решить самостоятельно. Не подсматривайте в `src/lib.rs`, пока не получите рабочую версию хотя бы на бумаге. После этого сравнивайте подход с разбором в README задачи: часто наивное решение компилируется, но содержит UB или плохо масштабируется под нагрузкой. Тесты в `tests/it.rs` запускайте через `cargo test -p <task-name>`, а где есть бенчмарки, прогоняйте `cargo bench`. Для задач с unsafe обязательно прогон под `cargo +nightly miri test`.

## 100 реальных вопросов с собеседований по Rust

Ниже собраны вопросы, которые задавали на собеседованиях в продуктовых и инфраструктурных компаниях. Разделены по темам, внутри темы упорядочены по возрастанию сложности.

### Владение, заимствование, лайфтаймы

1. Что такое move-семантика и чем она отличается от копирования в C++.
2. Когда тип реализует `Copy` автоматически, а когда нет, и почему `String` не `Copy`.
3. Объясните правило одного мутабельного или нескольких иммутабельных заимствований и зачем оно нужно.
4. Что такое NLL (non-lexical lifetimes) и какую проблему они решают по сравнению со старой моделью.
5. Зачем нужны явные лайфтаймы в сигнатурах функций, если компилятор и так умеет их выводить.
6. В чём разница между `&'a T` и `&'static T`, и когда `'static` не означает «живёт вечно».
7. Что такое lifetime elision rules и где они не работают.
8. Объясните variance: covariant, contravariant, invariant на примере `&'a T`, `&'a mut T` и `fn(&'a T)`.
9. Почему `Cell<T>` инвариантен по `T` и к чему привела бы ковариантность.
10. Что произойдёт, если вернуть ссылку на локальную переменную, и почему именно borrow checker это ловит.
11. Как реализовать структуру, которая хранит ссылку на собственное поле, и почему обычный путь невозможен.
12. Зачем нужен `PhantomData` и какие у него типичные применения.
13. Разница между `Drop` и обычным выходом из скобки в плане порядка освобождения ресурсов.
14. Что такое drop check (dropck) и какие проблемы он предотвращает.
15. Объясните паттерн «interior mutability» и когда выбирать `Cell`, `RefCell`, `UnsafeCell`.

### Типы, трейты, обобщения

16. Чем отличается `impl Trait` в позиции аргумента и в позиции возвращаемого значения.
17. Что такое dyn Trait, как устроен fat pointer и какова стоимость динамической диспетчеризации.
18. Когда трейт является объектно-безопасным и какие сигнатуры это нарушают.
19. Разница между associated types и generic parameters в трейтах, что выбрать для `Iterator`.
20. Что такое GAT (generic associated types) и какую задачу они решают на примере lending iterator.
21. Как работает orphan rule и зачем он нужен.
22. Что такое newtype pattern и где его реально применяют.
23. Объясните разницу между `From`, `Into`, `TryFrom`, `AsRef`, `Borrow`, `Deref`.
24. Когда стоит реализовывать `Deref` для пользовательского типа, а когда это антипаттерн.
25. Что такое coherence и почему компилятор иногда отказывается выводить `impl` для конфликтующих типов.
26. Как реализовать трейт для всех `T: Trait` и не получить конфликт с конкретными типами.
27. В чём разница между `Sized`, `?Sized`, `!Sized` и где встречаются DST.
28. Как работает специализация (`feature(specialization)`) и почему её до сих пор нет в stable.
29. Объясните higher-ranked trait bounds (`for<'a>`) на примере замыкания, принимающего ссылку.
30. Разница между `Fn`, `FnMut`, `FnOnce`, и как компилятор выбирает, что реализовать для замыкания.

### Конкурентность и параллелизм

31. Чем отличаются `Send` и `Sync` и почему `Rc` ни тот ни другой.
32. Почему `Mutex<T>` реализует `Send` независимо от `T`, но `Sync` только при `T: Send`.
33. Что такое memory ordering, опишите Relaxed, Acquire, Release, AcqRel, SeqCst на конкретных примерах.
34. В чём опасность `AtomicUsize` с `Ordering::Relaxed` при синхронизации двух потоков.
35. Объясните проблему ABA и как от неё защищаются в lock-free структурах.
36. Что такое poisoning у `Mutex` и как его правильно обрабатывать.
37. Чем `RwLock` отличается от `Mutex` и в каких случаях он медленнее.
38. Когда `parking_lot::Mutex` выигрывает у стандартного и почему.
39. Как реализовать spinlock на `AtomicBool` и где это допустимо.
40. Что такое false sharing и как его избежать на уровне структуры данных.
41. Объясните `crossbeam::epoch` и зачем нужны эпохи в реклеймации памяти.
42. Что такое `thread::scope` и какую проблему он решает по сравнению с `thread::spawn`.
43. Как передать `!Send` значение между потоками и почему это в принципе плохая идея.
44. Разница между `std::sync::mpsc`, `crossbeam::channel` и `tokio::sync::mpsc`.
45. Как корректно реализовать ленивую инициализацию глобального значения без `lazy_static`.

### Async и runtime

46. Что такое `Future` и почему `poll` принимает `Pin<&mut Self>`.
47. Объясните, как работает `async fn` под капотом и во что её разворачивает компилятор.
48. Что такое state machine у async-функции и почему её размер может неожиданно вырасти.
49. Зачем нужен `Pin` и какие гарантии он даёт.
50. Разница между `Unpin` и просто отсутствием `Pin`, и когда тип реально требует `!Unpin`.
51. Как написать self-referential структуру корректно через `Pin`.
52. Почему `tokio::spawn` требует `Send + 'static`, а `tokio::task::spawn_local` нет.
53. Что такое cooperative scheduling в tokio и как блокирующий вызов ломает рантайм.
54. Зачем нужен `spawn_blocking` и какие у него ограничения.
55. Объясните разницу между `select!`, `join!`, `try_join!` и когда какой использовать.
56. Что произойдёт, если уронить `JoinHandle` от `tokio::spawn`.
57. Как реализовать таймаут на любой `Future` без аллокаций.
58. Разница между `futures::stream::Stream` и `Iterator`, почему нет `for await`.
59. Что такое backpressure в стримах и как его реализовать.
60. Как сделать `Send` гард `MutexGuard` через `.await` и почему это опасно.
61. Объясните, почему `async fn` в трейтах долго не работала и что появилось в 1.75.
62. Как устроен `Waker`, и что произойдёт, если в `poll` забыть его сохранить.

### Unsafe, FFI, низкий уровень

63. Перечислите ровно те операции, которые требуют `unsafe`, и какие из них компилятор не проверяет.
64. Что такое UB в Rust и чем оно отличается от UB в C.
65. Объясните разницу между `*const T`, `*mut T`, `&T`, `&mut T` в плане алиасинга.
66. Что такое stacked borrows и tree borrows, и какую модель проверяет miri.
67. Когда корректно использовать `mem::transmute` и какие альтернативы существуют.
68. Что не так с `Vec::from_raw_parts`, если передать чужой указатель.
69. Как правильно реализовать `Drop` для типа, который владеет сырым указателем.
70. Объясните паттерн `ManuallyDrop` и где он необходим.
71. Что такое `#[repr(C)]`, `#[repr(transparent)]`, `#[repr(packed)]` и в каких случаях каждый.
72. Какие проблемы с alignment возникают при `#[repr(packed)]` и ссылках на поля.
73. Как организовать FFI с C-библиотекой, которая возвращает указатель на ресурс с собственным деструктором.
74. Почему нельзя просто кастовать `&[u8]` в `&str` через transmute.
75. Что такое provenance у указателя и зачем ввели `strict_provenance`.
76. Как корректно реализовать кастомный аллокатор и какие инварианты он обязан соблюдать.
77. Что такое soundness, и почему `unsafe` блок не делает функцию unsound.
78. Разберите, почему `mem::uninitialized` deprecated в пользу `MaybeUninit`.

### Производительность

79. Как влияет монорфизация на размер бинарника и время компиляции.
80. Когда стоит использовать `Box<dyn Trait>` ради сокращения кода, а не ради динамики.
81. Объясните разницу между stack и heap аллокацией, и почему `Vec<T>` всегда на куче.
82. Что такое small string optimization и почему в стандартном `String` её нет.
83. Когда `#[inline]`, `#[inline(always)]`, `#[inline(never)]` действительно влияют на производительность.
84. Как читать вывод `cargo asm` и на что обращать внимание при оптимизации горячего цикла.
85. Что такое LTO и thin LTO, какие у них trade-off.
86. Как профилировать Rust-программу под Linux: `perf`, `flamegraph`, `cargo-flamegraph`.
87. Когда `Arc::clone` становится узким местом и чем его заменить.
88. Объясните bounds check elimination и как помочь компилятору его сделать.
89. Что такое SIMD в Rust, разница между `std::simd` и `std::arch`.

### Макросы и метапрограммирование

90. Разница между `macro_rules!` и proc-макросами, когда что выбирать.
91. Что такое hygiene в макросах и какие проблемы она решает.
92. Объясните фрагменты `expr`, `ty`, `pat`, `ident`, `tt` и где они различаются.
93. Как написать derive-макрос, который добавляет реализацию трейта со своим состоянием.
94. Что такое `syn`, `quote`, `proc-macro2` и зачем последний нужен отдельно.
95. Какие ограничения у атрибутных макросов по сравнению с derive.

### Архитектура и дизайн API

96. Когда выбирать typestate pattern, а когда обойтись runtime-проверками.
97. Объясните builder pattern в Rust и его вариант с consuming `self`.
98. Как спроектировать публичный API так, чтобы добавление поля в структуру не было breaking change.
99. Что такое sealed trait и как его реализовать на стабильном Rust.
100. Как организовать обработку ошибок в библиотеке: `thiserror`, `anyhow`, кастомный enum, и где какой подход уместен.

## Лицензия

MIT. Подробнее см. файл `LICENSE`.

## Шпаргалка к подготовке

Этот раздел собран как короткие напоминалки на ту неделю перед собеседованием, когда нет времени перечитывать книги и хочется освежить ключевые механизмы. Идёт по тем же восьми темам, что и список вопросов выше, но без длинных объяснений. Просто факты, на которые часто наступают.

### Владение и лайфтаймы

Правило одно. У значения один владелец. После move старая переменная невалидна. Copy типы при присваивании копируются и оба места остаются валидны. Copy реализуют только типы без Drop. Drop руками не вызывают, для досрочного освобождения есть свободная функция std::mem::drop. Заимствование. Либо много &T, либо одна &mut T. NLL считает лайфтаймы до последнего использования, а не до конца блока. Лайфтайм 'static в ссылках это вся программа, в bound T: 'static это отсутствие коротких ссылок внутри типа. Самоссылочные структуры либо через Pin и unsafe, либо переписать через индексы и арены.

### Типы и трейты

Generics мономорфизируются и инлайнятся, dyn Trait это vtable плюс косвенный вызов. Object safety требует, чтобы методы не принимали и не возвращали Self по значению и не имели своих generic параметров. Ассоциированный тип фиксирован в реализации, generic параметр трейта может быть разным. Orphan rule обходят через newtype. Blanket impl даёт бесплатные Into из From и ToString из Display. Sized стоит по умолчанию, для DST пишут T: ?Sized. impl Trait в позиции возврата это конкретный непоименованный тип, в позиции аргумента это сахар над T: Trait. #[non_exhaustive] на enum позволяет потом добавлять варианты без поломки semver.

### Конкурентность

Send это безопасная передача в другой поток. Sync это &T можно делить между потоками. Rc и RefCell не Send. Arc и Mutex Send и Sync при подходящем T. Mutex дешевле RwLock на коротких секциях. Poisoning в std Mutex после паники под локом, в parking_lot poisoning нет. Atomic ordering. Relaxed для счётчиков, Acquire и Release для flag паттернов, SeqCst если действительно нужен глобальный порядок. Каналы из std::sync::mpsc unbounded, для backpressure нужны bounded из crossbeam или tokio. std::thread::scope разрешает заимствование стека без 'static. Rayon хорош на CPU bound, плох с блокирующими вызовами внутри par_iter. Дедлок чаще всего из за разного порядка локов или удержания лока через await.

### Async

async fn возвращает impl Future и ничего не делает до первого poll. Future это poll возвращающий Ready или Pending плюс контракт разбудить waker, когда сможет продвинуться. Tokio это де факто стандарт. Внутри async никогда нельзя блокировать поток, для CPU и sync IO есть tokio::task::spawn_blocking. tokio::select отменяет проигравшие ветки на месте, поэтому нужны cancel safe операции. Локи через await это рецепт дедлока. Структурированную конкурентность собирают через JoinSet, FuturesUnordered, async-scoped. Backpressure через bounded mpsc, который тормозит send. Pin нужен для самоссылочных стейт машин. Если своя фьюча со сложными полями, используется pin_project и никакого ручного unsafe.

### Unsafe и FFI

Unsafe разрешает разыменование сырых указателей, вызов unsafe функций, доступ к mutable static, реализацию unsafe трейтов и union. Borrow checker не отключается. UB чаще всего из за алиасинга, гонок данных, висячих указателей, чтения неинициализированной памяти, нарушения инвариантов примитивов. Для неинициализированной памяти MaybeUninit, ни в коем случае не mem::uninitialized. Для интерьорной мутабельности UnsafeCell под капотом. Для FFI всегда #[repr(C)], для newtype через границу repr(transparent). Паника через extern C это UB или abort, оборачивают в catch_unwind. Безопасный публичный API над unsafe документирует свои инварианты в разделе Safety doc комментария и покрывается Miri.

### Производительность

Сначала измерить через perf и flamegraph, потом править. Для микробенчей criterion с black_box. Аллокации главный источник тормозов на горячем пути. Используют with_capacity, переиспользуемые буферы, smallvec, Cow вместо String. HashMap по умолчанию SipHash, для внутренних карт ahash или fxhash в 2 3 раза быстрее. Локальность данных важнее микрооптимизаций. Vec<T> с маленькими T быстрее Vec<Box<T>>. SoA выигрывает у AoS на узких проходах. LTO fat и codegen-units = 1 в release дают 5 15 процентов. PGO ещё столько же на правильной нагрузке. Глобальный аллокатор jemalloc или mimalloc часто бесплатное ускорение. Inline атрибут обязателен на публичных мелких функциях в библиотеке.

### Макросы и метапрограммирование

Декларативные macro_rules для шаблонов и DSL, гигиена есть для локальных имён, но в путях типов лучше писать $crate. Процедурные макросы делятся на function like, derive и attribute, под капотом TokenStream в TokenStream. Внутри обычно syn для разбора, quote для генерации, proc-macro-error для диагностики. Build script build.rs для генерации кода, компиляции C, схем protobuf, результаты кладут в OUT_DIR. const fn растёт по возможностям с каждым релизом, в нём уже можно if, циклы, многое из стандартной библиотеки. Typestate кодирует состояние объекта в типе. Builder это его прикладной случай.

### Архитектура и дизайн API

На уровне библиотеки ошибки через thiserror и осмысленный enum, на уровне приложения anyhow с контекстом. Публичный API возвращает impl Trait и принимает impl AsRef или impl IntoIterator, чтобы не заставлять пользователя конвертировать. Трейты с минимальным набором методов проще реализовывать сторонним типам. Semver. Удаление публичного, изменение сигнатуры, новые обязательные методы и параметры это breaking. Новые варианты в #[non_exhaustive] enum это не breaking. Большой проект режется на cargo workspace по обязанностям. Чистая логика без IO в отдельном crate, IO и транспорт сверху, bin тонкий.

### Что почитать и попробовать руками

The Rust Programming Language. Базовая книга. Rust by Example. Подборка коротких примеров. Rust Atomics and Locks. Лучший разбор всего, что связано с памятью и многопоточностью. Async Book. Объясняет, как работает async изнутри. Документация tokio. Часто отвечает быстрее, чем поиск по статьям. Crate cargo expand. Показывает, во что разворачивается код после макросов. Miri. Ловит UB в unsafe коде. Cargo clippy. Должен быть в CI любого проекта. Этой минимальной обвязки хватает для подготовки и для повседневной работы.

